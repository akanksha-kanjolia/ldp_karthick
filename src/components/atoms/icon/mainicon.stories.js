import React from 'react';
import MainIcon from './mainicon';

export default {
    title: 'Blinkist/Atoms/MainIcon',
    component: MainIcon,
}

const Template = args => <MainIcon {...args} />

export const HeaderIcon = Template.bind({})
